{
  "name": "val-ark-screenshots",
  "private": true,
  "scripts": {
    "test": "npx playwright test",
    "install-browsers": "npx playwright install chromium"
  },
  "devDependencies": {
    "@playwright/test": "^1.48.0"
  }
}
